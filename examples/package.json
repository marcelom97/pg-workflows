{
  "name": "pg-workflows-examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:lib": "test -d ../dist || (cd .. && bun run build)",
    "start": "npm run build:lib && dotenvx run -- tsx basic.ts",
    "example:basic": "npm run build:lib && dotenvx run -- tsx basic.ts",
    "example:approval-flow": "npm run build:lib && dotenvx run -- tsx approval-flow.ts",
    "example:timeout": "npm run build:lib && dotenvx run -- tsx timeout.ts",
    "example:polling": "npm run build:lib && dotenvx run -- tsx polling.ts",
    "example:cron": "npm run build:lib && dotenvx run -- tsx cron.ts"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "pg-boss": ">=12.0.0",
    "pg-workflows": "file:.."
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.52.0",
    "tsx": "^4.19.0",
    "@types/pg": "^8.11.10"
  }
}
